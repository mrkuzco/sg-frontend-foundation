{
  "name": "sg-frontend-foundation",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "figma": "pnpm figma:styles && pnpm figma:variables",
    "figma:styles": "tsx scripts/sync-figma-tokens.ts",
    "figma:variables": "pnpm --filter @sg-foundation/ui generate-tokens"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
