import { colors } from "../themes/mystore/primitives";
import { fontFamily, fontSize } from "../themes/mystore/typography";
import { borderRadius } from "../tokens/spacing";

export const mystorePreset = {
  colors,
  fontFamily,
  fontSize,
  borderRadius,
} as const;
